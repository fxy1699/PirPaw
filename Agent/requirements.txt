# DyberPet Agent 系统依赖清单
# ==========================================

# 核心必需依赖
# ------------------------------------------
qwen-agent>=0.0.27          # Qwen Agent SDK - 核心AI对话功能
python-dotenv>=1.0.0        # 环境变量配置支持
psutil>=5.9.0               # 系统信息监控

# 可选功能依赖
# ------------------------------------------
# 屏幕分析功能 (Vision模块)
pyautogui>=0.9.54           # 屏幕截取和自动化
Pillow>=10.0.0              # 图像处理库

# OCR文字识别 (可选增强)
paddleocr>=2.7.0            # OCR文字识别引擎

# 摄像头和姿态检测 (Camera模块)
opencv-python>=4.8.0        # 计算机视觉库
# 注意：MediaPipe对Python版本有严格要求，支持Python 3.8-3.11
mediapipe>=0.9.0; python_version >= "3.8" and python_version < "3.12"  # 姿态检测和手势识别

# Windows系统摄像头支持
pywin32>=306; sys_platform == "win32"  # Windows API支持

# 应用追踪功能 (Tracker模块)
# 无额外依赖，使用系统内置API

# 开发和调试工具 (可选)
# ------------------------------------------
requests>=2.31.0           # HTTP请求 (用于API测试)
pytest>=7.4.0              # 单元测试框架
black>=23.0.0               # 代码格式化工具
flake8>=6.0.0               # 代码质量检查

# API服务密钥说明
# ------------------------------------------
# 以下API密钥需要在环境变量中配置：
# 
# QWEN_API_KEY=sk-xxx         # Qwen DashScope API密钥 (必需)
#   获取地址: https://dashscope.aliyun.com/
# 
# SERPER_API_KEY=xxx          # Serper搜索API密钥 (可选)
#   获取地址: https://serper.dev/
# 
# AMAP_TOKEN=xxx              # 高德天气API密钥 (可选)
#   获取地址: https://console.amap.com/
# 
# WEATHER_API_KEY=xxx         # OpenWeatherMap API密钥 (备用)
#   获取地址: https://openweathermap.org/api 